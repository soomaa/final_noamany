/** Default password for new gym member app accounts (api_users). 6 digits to satisfy the ≥6 rule. */
export const MEMBER_DEFAULT_PASSWORD = '000000';

/** Minimum length for a member's chosen password. */
export const MEMBER_MIN_PASSWORD_LENGTH = 6;
